# TurfFlow Sports Arena - Repository Guidelines

## Code Style & Architecture
- **Framework**: TanStack Start + React 19 + TypeScript.
- **Routing**: File-based routing located in `src/routes/`.
- **Styling**: Tailwind CSS v4 + Vanilla CSS extensions in `src/styles.css`.
- **Components**: Modular UI components organized under `src/components/`.
